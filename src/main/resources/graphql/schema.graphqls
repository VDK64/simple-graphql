type Query {
    user(id: ID): User
    users: [User]
}

type User {
    id: ID!
    age: Int!
    firstName: String!
    lastName: String!
    email: String!
    subscriptions: [Subscription]
}

type Subscription {
    id: ID!
    name: String!
}